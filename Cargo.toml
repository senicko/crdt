[package]
name = "crdt"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.14.3"
tokio = { version = "1.52.3", features = ["macros", "rt-multi-thread"] }
tokio-stream = "0.1.18"
tonic = "0.14.6"
tonic-prost = "*"

[features]
default = []
server = []
client = []
full = ["server", "client"]

[build-dependencies]
tonic-prost-build = "*"
